# 依赖目录
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 生产运行时用户数据 (容器挂载目录，不纳入版本控制以保护密码隐私)
data/*
!data/devices/
!data/devices/.gitkeep

# 临时文件与日志
*.log
# 仅忽略仓库根目录下的临时调试脚本；正式回归测试位于 tests/ 且必须纳入版本控制
/test_*.js
*.tmp
.DS_Store
Thumbs.db
.last_firm_auth.json

# 本地旧版前端备份 (不入库)
app/static_backup_v1/

# 本地分析产物与会话还原记录 (含敏感信息，绝不入库)
analysis/

# WorkBuddy 本地项目数据 (记忆 / 技能等，非代码，不入库)
.workbuddy/
